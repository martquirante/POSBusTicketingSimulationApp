<?xml version="1.0" encoding="utf-8"?>
<!--
  This drawable defines the background for messages received from other users.

  How it works:
  - It's similar to the sent message background but uses a different color (a light gray).
  - The corners are rounded differently to create a chat bubble that points from the left.
    The bottom-left corner remains sharp.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#E0E0E0" />
    <corners
        android:topLeftRadius="16dp"
        android:topRightRadius="16dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="16dp" />
</shape>